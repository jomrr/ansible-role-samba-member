---
# role: ansible-role-samba-member
# file: molecule/podman/molecule.yml

dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: podman
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: default-samba-member
    image: ${MOLECULE_IMG:-"jam82/molecule-debian:latest"}
    command: ${MOLECULE_IMG_CMD:-""}
    pre_build_image: true
    tty: false
    tmpfs:
      - /tmp
      - /run
    volumes:
      - "/sys/fs/cgroup:/sys/fs/cgroup:ro"
provisioner:
  name: ansible
  log: true
  playbooks:
    prepare: ../resources/playbooks/prepare.yml
    converge: ../resources/playbooks/converge.yml
    verify: ../resources/playbooks/verify.yml
  inventory:
    group_vars:
      all:
        samba_member_role_enabled: true
verifier:
  name: ansible
